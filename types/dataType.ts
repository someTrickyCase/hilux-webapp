export type dataType = {
    title: string;
    logo: string;
    img: string;
    link: string;
};

export type FeedCardType = {
    article?: string;
    images: string[] | string;
    price: string;
    title: string;
    description?: string;
};
